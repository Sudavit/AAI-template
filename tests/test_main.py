# Goal: Invoke main() to cover the print statement logic.

from AAI.main import main

def test_main_executes_successfully():
    # Calling the function ensures the internal lines are 'covered'
    # We pass None to match your signature's default
    main(None)
